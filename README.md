-e # AI Agent Project

Welcome to the AI Agent running inside Termux on Android!

## Structure
- `app.py` & `chat.py`: Core application logic
- `save_chat.py`: Chat history management
- `static/` & `templates/`: Frontend assets
- `data/`: Local database/data storage

## Setup
Run `pip install -r requirements.txt` to install dependencies.

